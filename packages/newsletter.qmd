# Newsletter

```{r, echo=FALSE, results='asis', message=FALSE, warning=FALSE}
library(lubridate)
source(here::here("scrapePackages.R"))

period <- seq.Date(from = as.Date("2025-01-01"), to = Sys.Date(), by = 1) |>
  rev() |>
  format("%B %Y") |>
  unique()

readr::read_csv(here::here("packages", "packages.csv"), show_col_types = FALSE) |>
  dplyr::filter(.data$dev == "ox") |>
  dplyr::select(!"dev") |>
  createNewsletter(period = period) |>
  cat()
```

## Subscribe to Our Newsletter

```{=html}
<form id="subscribe-form">
  <input type="email" name="email" required placeholder="Enter your email" />
  <button type="submit">Subscribe</button>
</form>

<div id="message" style="display:none; color: green; font-weight: bold;">
  ✔ You've been subscribed to the Oxinfer Newsletter :)
</div>

<script>
  document.getElementById('subscribe-form').addEventListener('submit', function(event) {
    event.preventDefault(); // Stop normal form submission

    const form = event.target;
    const formData = new FormData(form);

    fetch('https://script.google.com/macros/s/AKfycbwPL438sqcd6WOqiKUMeD0OsT0QoSUAU5efR3Tj6rjggtdAoU8JqJjIEJnOJPjkB-8/exec', {
      method: 'POST',
      body: formData
    })
    .then(response => response.text())
    .then(text => {
      // Hide the form and show the success message
      form.style.display = 'none';
      document.getElementById('message').style.display = 'block';
    })
    .catch(error => {
      alert('Oops, something went wrong. Please try again.');
      console.error(error);
    });
  });
</script>
```
